// Statements are read, parsed and analysed entirely in the browser — nothing is
// ever sent to a server, so there is nothing to render on one.
export const ssr = false;
export const prerender = true;
